pipeline {
    agent any

    tools {
        jdk 'JDK'
        maven 'Maven3'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                url: 'https://github.com/Yash28102/Final_Assessment.git'
            }
        }

        stage('Verify Workspace') {
            steps {
                bat 'dir'
                bat 'dir /s'
            }
        }
	stage('Compile') {
            steps {
				dir('Final Assessment'){
                bat 'mvn compile'
            }
        }
        }
        stage('Build and Test') {
            steps {
                dir('Final Assessment') {
                    bat 'mvn clean'
                }
            }
        }
 
        stage('Test') {
            steps {
				dir('Final Assessment'){
                bat 'mvn test'
            }
        }
        }
 
        

        stage('Package') {
            steps {
                dir('Final Assessment') {
                    bat 'mvn package -DskipTests'
                }
            }
        }

        stage('Docker Build') {
            steps {
                dir('Final Assessment') {
                    bat 'docker build -t testng-framework .'
                }
            }
        }
        stage('Docker Run') {
            steps {
                dir('Final Assessment') {
                    bat 'docker run -rm testng-framework'
                }
            }
        }
    }
	
    post {
        always {
            junit '**/target/surefire-reports/*.xml'
            archiveArtifacts '**/target/*.jar'
        }
    }
}
